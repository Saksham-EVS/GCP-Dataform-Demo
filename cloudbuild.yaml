logsBucket: "gs://x-to-bq-training/bq-dataform_demo/logs"

steps:
    - id: "Dataform CI"
      name: "node:20"
      entrypoint: bash
      args:
        - -c
        - |
          npm install -g @dataform/cli
          dataform --version
          dataform compile
          dataform test
